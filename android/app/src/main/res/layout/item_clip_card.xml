<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_clip"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="16dp"
    android:layout_marginEnd="16dp"
    android:layout_marginBottom="10dp"
    app:cardBackgroundColor="@color/surface_card"
    app:cardCornerRadius="10dp"
    app:cardElevation="0dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Clip Content Text -->
        <TextView
            android:id="@+id/tv_clip_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="14sp"
            android:maxLines="3"
            android:ellipsize="end" />

        <!-- Expand / Collapse Button -->
        <TextView
            android:id="@+id/btn_show_more"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/show_more"
            android:textColor="@color/accent_brown_light"
            android:textSize="12sp"
            android:paddingTop="6dp"
            android:paddingBottom="2dp"
            android:visibility="gone" />

        <!-- Bottom Action Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="10dp">

            <!-- Pinned Badge -->
            <TextView
                android:id="@+id/tv_pinned_badge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/pinned_badge"
                android:textColor="@color/accent_brown"
                android:textSize="11sp"
                android:textStyle="bold"
                android:visibility="gone" />

            <!-- Source Device -->
            <TextView
                android:id="@+id/tv_clip_source"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:textColor="@color/text_muted"
                android:textSize="11sp" />

            <!-- Pin / Unpin Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_pin"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="wrap_content"
                android:layout_height="32dp"
                android:text="@string/pin_action"
                android:textColor="@color/text_muted"
                android:textSize="11sp"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                app:backgroundTint="@color/bg_dark" />

            <!-- Delete Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_delete"
                style="@style/Widget.Material3.Button.TonalButton"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginStart="6dp"
                android:text="@string/delete_action"
                android:textColor="@color/status_red"
                android:textSize="11sp"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:padding="0dp"
                app:backgroundTint="@color/bg_dark" />

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
